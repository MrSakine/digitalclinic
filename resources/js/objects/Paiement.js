class Paiement {
    /**
     * 
     * @param {Date} datePaiement 
     * @param {Number} montant 
     */
    constructor(datePaiement, montant) {
        this.datePaiement = datePaiement
        this.montant = montant
    }
}